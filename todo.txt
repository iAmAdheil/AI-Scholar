bugs:
scrolling methods don't work -> most probably due to old - new arch

android setup:
add mic permissions for android
add new arch to android

features:
pass prev messages for chat context
track token usage for each user